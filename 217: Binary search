This program will implement a one-dimensional array, take a number form users to search for in the array using Binary Search. If the number is found print "SUCCESSFUL SEARCH" else print "UNSUCCESSFUL SEARCH".

Example:

Sample Inputs	
4
1
5
9
7
5
5	

Sample Outputs
SUCCESSFUL SEARCH

#include <stdio.h>
int main()
{
  int c, first, last, middle, n, search, array[100],temp;
  scanf("%d", &n);

  for (c = 0; c < n; c++)
    scanf("%d", &array[c]);
  scanf("%d", &search);
  
  for(int i=0;i<n-1;i++){
         for(int j=i+1;j<n;j++){
             if(array[i]>array[j]){
                 temp=array[j];
                 array[j]=array[i];
                 array[i]=temp;
             }
         }
     }
    
  first = 0;
  last = n - 1;
  middle = (first+last)/2;

  while (first <= last) {
    if (array[middle] < search)
      first = middle + 1;
    else if (array[middle] == search) {
      printf("SUCCESSFUL SEARCH");
      break;
    }
    else
      last = middle - 1;

    middle = (first + last)/2;
  }
  if (first > last)
    printf("UNSUCCESSFUL SEARCH");

  return 0;

} 
