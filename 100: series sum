Develop a program to calculate the following Series: Sum=1-x2/2! +x4/4!-x6/6! +x8/8!-x10/10!

Sample Inputs	
1
Sample Outputs
1	1.000000

#include <stdio.h>
 #include <math.h>
 int fact(int n){
     if(n==1 || n==0)
         return 1;
     else
        return n*fact(n-1);
 }
 int main()
 {
 int a,s;
 float sum=1;
 scanf("%d %d", &a,&s);
 
 for(int i=1;i<s;i++){
     if(i%2!=0){
         sum=sum-(pow(a,2*i)/fact(2*i));
     }
     else
         sum=sum+(pow(a,2*i)/fact(2*i));
 }
 printf("%f", sum);
 return 0;
 }
 
 
 
