Given coefficients of quadratic equation a, b and c, Develop a program to compute the roots of a quadratic equation. Print appropriate messages.

Example:

Sample Inputs	Sample Outputs
1
2
1	roots are real and equal
root1= -1.000000
root2= -1.000000


 #include <math.h>
#include <stdio.h>
int main() {
    double a, b, c, discriminant, root1, root2, realPart, imagPart;
    scanf("%lf%lf%lf",&a,&b,&c);

    discriminant = b * b - 4 * a * c;
    
    if (discriminant > 0) {
        printf("roots are real and distinct\n");
        root1 = (-b + sqrt(discriminant)) / (2 * a);
        root2 = (-b - sqrt(discriminant)) / (2 * a);
        printf("root1= %.6lf\nroot2= %.6lf", root1, root2);
    }

    else if (discriminant == 0) {
        printf("roots are real and equal\n");
        root1 = root2 = -b / (2 * a);
        printf("root1= %.6f\nroot2= %.6lf", root1,root1);
    }

    else {
        printf("roots are complex\n");
        realPart = -b / (2 * a);
        imagPart = sqrt(-discriminant) / (2 * a);
        printf("root1= %.6lf+i%.6lf root2= %.6f-i%.6f", realPart, imagPart, realPart, imagPart);
    }

    return 0;
} 
