Description:
Write a C program using a linear search check for a value that is present in a given list of integers and print the location of the number if the number is present.

Sample Inputs	
5
3
2
1
5
4
5	

Sample Outputs
5 is present at location 4


 //Please accept the inputs from the console and print the output
 #include <stdio.h>
 
 int main()
 {
     int n;
     scanf("%d",&n);
     int arr[n], k;
     for(int i=0;i<n;i++)
        scanf("%d", &arr[i]);
     scanf("%d",&k);
     for(int i=0;i<n;i++){
         if(k==arr[i]){
             printf("%d is present at location %d",k,i+1);
             return 0;
         }
     }
     printf("%d is not present in the list", k);
     return 0;
 }
 
