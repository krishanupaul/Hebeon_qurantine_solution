Develop a program to introduce 2D Array manipulation and implement Matrix multiplication and ensure the rules of multiplication are checked.
Sample Input: 2 2 2 2 1 2 3 4 1 2 3 4
Sample Output: 7 10 15 22


#include<stdio.h>

int main(void)
{
  int c, d, p, q, m, n, k, tot = 0;
  int fst[10][10], sec[10][10], mul[10][10];

  scanf("%d%d", &m, &n);
  scanf(" %d %d", &p, &q);
  
  for (c = 0; c < m; c++)
    for (d = 0; d < n; d++)
      scanf("%d", &fst[c][d]);
 for (c = 0; c < p; c++)
      for (d = 0; d < q; d++)
        scanf("%d", &sec[c][d] );
        
        
  if (n != p){
    printf("Invalid");
    return 0;
  }    
  else
  {
    for (c = 0; c < m; c++) {
      for (d = 0; d < q; d++) {
        for (k = 0; k < p; k++) {
          tot = tot + fst[c][k] * sec[k][d];
        }
        mul[c][d] = tot;
        tot = 0;
      }
    }
    for (c = 0; c < m; c++) {
      for (d = 0; d < q; d++)
        printf("%d\n", mul[c][d]);
    }
  }
  return 0;
}
