Develop a program that uses a recursive function to generate Pascal’s Triangle

Sample Inputs	
4	

Sample Outputs
1 1
1 2 1
1 3 3 1


 //Please accept the inputs from the console and print the output
 #include <stdio.h>

 void pascal(int n){
     if(n==1)
         return 1;
     else
         printf("%d",pow(11,pascal(n-1)));
 }
 
 int main()
 {
 int a;
 scanf("%d", &a);
 pascal(a);
 return 0;
 }
 
