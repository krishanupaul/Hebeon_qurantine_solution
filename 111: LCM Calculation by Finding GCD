Develop a program to find the least common multiple (LCM) of two positive integers a and b is the smallest integer that is evenly divisible by both a and b.

Sample Inputs	
4 8	

Sample Outputs
8


#include <stdio.h>
int main() {
    int n1, n2, min;
    scanf("%d %d", &n1, &n2);

    min = (n1 > n2) ? n1 : n2;

    while (1) {
        if (min % n1 == 0 && min % n2 == 0) {
            printf("%d",min);
            break;
        }
        ++min;
    }
    return 0;
}
